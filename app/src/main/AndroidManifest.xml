<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVCTutorial">
        <activity android:name=".dagger2.sandbox.SandBoxActivity"
            android:exported="false"/>
        <activity
            android:name=".compose.ComposeActivity"
            android:exported="false"
            android:label="@string/title_activity_compose"
            android:theme="@style/Theme.MVCTutorial" />
        <activity
            android:name=".network.RetrofitActivity"
            android:exported="false" />
        <activity
            android:name=".di_pre.DIActivity"
            android:exported="true" />
        <activity
            android:name=".navigate.NavigateActivity"
            android:exported="true" />
        <activity
            android:name=".uitest.UiTestActivity"
            android:exported="true" />
        <activity
            android:name=".homecontent.BannerActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>